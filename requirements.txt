# Runtime (pyproject.toml es la fuente de verdad; esto lo espeja para Docker)
pydantic>=2.7
pydantic-settings>=2.4
google-genai>=0.1.1
fastapi>=0.115
httpx>=0.27
cryptography>=42
apscheduler>=3.10
groq>=0.4.0
requests>=2.32
temporalio>=1.9.0

# Opcionales (descomentar si se usan)
# supabase>=2.0.0
# psycopg[binary]>=3.1.0

# Dev (tests/lint) — normalmente cubierto por `pip install -e ".[dev]"`
# pytest
# pytest-asyncio
# pytest-cov
# ruff
# mypy