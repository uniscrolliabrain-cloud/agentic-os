hardening: contracts, security and error-handling across kernel, connectors, execution and interfaces

- WebhookEvent y AuditRecord: reemplaza la construccion invalida
  `BaseModel := Any` por modelos Pydantic v2 reales con validacion
- Drive: path traversal corregido con comparacion semantica
  (pathlib.is_relative_to) en lugar de str.startswith
- Tools: error uniforme ToolValidationError; ninguna tool devuelve
  {"error": ...} que el Executor reportaria como success
- Executor: distingue ToolCompleted de ToolFailed (success True/False)
- Ontologia: Entity/Relation con validadores (kind canonico, ids no
  vacios, sin auto-relaciones); invariantes ejecutables
  (OntologyViolation) y OntologyValidator integral (estructura,
  vocabulario, referencias, semantica, invariantes)
- PlanStep.params tipado dict[str, object]
- MCP: MCPTool con input_schema y populate_by_name; MCPServer.call
  valida registro y contratos
- AgentRegistry y Memory: contratos completos (register/get/exists/
  remove; MemoryItem/MemoryStore.put/get/search; retriever determinista
  sobre el store)
- CredentialStore: bug critico corregido - Secret se destruida al
  guardar (str(Secret) = **********); ahora get_secret_value();
  documentado explicitamente que base64 NO es cifrado
- Reloj canonico: eliminados todos los datetime.utcnow() en favor de
  kernel.types.time.now_utc()
- _map_kind_to_action: heuristica de substrings reemplazada por
  ACTION_BY_KIND determinista
- clinic/finance: proyecciones con modelos explicitos y estado
  NOT_IMPLEMENTED honesto (nunca respuestas vacias fingidas)
- webhook/scheduler/pipelines: except Exception: pass eliminado; los
  fallos se loguean
- ruff clean; suite completa 147 passed

Tests nuevos: webhook_event, drive_path_safety, documentation_tool,
ontology_contracts, registry_memory_contracts, mcp_contract,
credential_store, kind_mapping, projections
